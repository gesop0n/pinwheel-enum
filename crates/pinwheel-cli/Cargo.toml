[package]
name = "pinwheel-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "pinwheel"
path = "src/main.rs"

[dependencies]
pinwheel-core = { path = "../pinwheel-core" }
clap = { version = "4", features = ["derive"] }
