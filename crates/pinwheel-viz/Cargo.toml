[package]
name = "pinwheel-viz"
version = "0.1.0"
edition = "2024"

[dependencies]
pinwheel-core = { path = "../pinwheel-core" }
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
layout-rs = "0.1"
